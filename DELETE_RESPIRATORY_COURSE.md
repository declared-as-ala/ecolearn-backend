# حذف دورة الجهاز التنفسي - دليل الإدارة

## 📋 نظرة عامة

تم حذف دورة "المحافظة على سلامة الجهاز التنفسي" من المنصة بشكل كامل. هذا الملف يوضح ما تم حذفه وكيفية تنظيف قاعدة البيانات.

## ✅ ما تم حذفه من الكود

### Frontend:
- ✅ `frontend/lib/courses/grade6/respiratorySafety.ts` - ملف الدورة الكامل
- ✅ إزالة الدورة من `frontend/lib/coursesData.ts`
- ✅ تحديث نص وصف السنة السادسة في صفحة اختيار المستوى

### Backend:
- ✅ إزالة الدورة من `backend/seed.js`
- ✅ إزالة الدورة من `backend/seedEnvironmental.js`
- ✅ إزالة الدورة من `backend/data/fallbackCourses.js`

## 🗄️ تنظيف قاعدة البيانات

لحذف جميع البيانات المرتبطة بالدورة من قاعدة البيانات، قم بتشغيل:

```bash
cd backend
node deleteRespiratoryCourse.js
```

### ما يقوم به الـ Script:

1. **حذف الدورة** من جدول `courses`
2. **حذف جميع سجلات التقدم** (Progress) المرتبطة بالدورة
3. **إزالة الشارات** (Badges) المرتبطة بالدورة من حسابات المستخدمين
4. **حذف الإشعارات** (Notifications) المرتبطة بالدورة
5. **التحقق** من عدم وجود بيانات متبقية

### Course IDs التي تم البحث عنها:

- `respiratory-system-safety`
- `respiratory-safety-6`
- `respiratory-system-safety-6`

### Badge المرتبط:

- `بطل الهواء النقي` 🌬️

## ⚠️ تحذيرات مهمة

1. **احتفظ بنسخة احتياطية** من قاعدة البيانات قبل تشغيل script الحذف
2. **تأكد من الاتصال** بقاعدة البيانات الصحيحة
3. **راجع النتائج** بعد تشغيل الـ script للتأكد من الحذف الكامل

## 📊 التحقق من الحذف

بعد تشغيل الـ script، تحقق من:

1. ✅ الدورة لم تعد تظهر في قائمة الدورات للسنة السادسة
2. ✅ لا توجد سجلات Progress مرتبطة بالدورة
3. ✅ الشارات تم إزالتها من حسابات المستخدمين
4. ✅ الإشعارات المرتبطة تم حذفها
5. ✅ Dashboard المدرس لا يعرض أي إشارة للدورة

## 🔄 في حالة الحاجة للتراجع

إذا كنت بحاجة لاستعادة الدورة:

1. استرجع نسخة احتياطية من قاعدة البيانات
2. أعد إضافة الدورة من ملفات seed الأصلية
3. قم بتشغيل `node seedEnvironmental.js` لإعادة إنشاء الدورة

## 📝 ملاحظات

- تم حذف جميع المحتويات المرتبطة بالدورة (فيديوهات، تمارين، ألعاب)
- لا توجد ملفات فيديو مرتبطة بهذه الدورة في مجلد `public/videos`
- تم تحديث جميع الملفات ذات الصلة

---

**تاريخ الحذف:** $(date)
**تم بواسطة:** System Admin
**الحالة:** ✅ مكتمل

